export enum AdvertiseType {
  Purchase = 'purchase',
  Sale = 'sale',
}

export enum AdvertiseCategoryType {
  Product = 1,
  Input = 2,
}

export enum AdvertiseStatusType {
  Approved = 'Approved',
  AutoExpired = 'AutoExpired',
  Cancelled = 'Cancelled',
  Declined = 'Declined',
  Expired = 'Expired',
  InActive = 'InActive',
  InProcess = 'InProcess',
  None = 'None',
  Pending = 'Pending',
  Published = 'Published',
  WaitingForPublishDate = 'WaitingForPublishDate',
  PendingPayment = 'PendingPayment',
}

export enum AdressIdType {
  NewAddress = 0,
}
